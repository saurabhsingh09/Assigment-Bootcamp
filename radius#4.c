#include<stdio.h>
#include<conio.h>
int main()
{
    float Area,r;
    printf(" Radius of circle: ");
    scanf("%f",&r);
    Area=3.14*r*r;
    printf("Area of circle is %f having the radius %f",Area,r);
    return 0;
}
